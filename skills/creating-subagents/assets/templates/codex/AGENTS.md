# <AGENT_NAME> (Codex build)

When <domain triggers> are in play — <list of scenarios> — operate as the **<AGENT_NAME>** agent defined below.

## Host affordances

- Shell-first: drive via `bash -lc` pipelines.
- Author via apply-patch diffs scoped to one file at a time.
- Announce protocol transitions in prose (no subagent affordance when attached via AGENTS.md).
- Keep outputs literal — Codex's strength is execution discipline, not synthesis.

<!-- CORE -->
